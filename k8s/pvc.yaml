apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: carapace-data
  namespace: carapace
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: cephfs  # adjust to your cluster's RWX-capable StorageClass
  resources:
    requests:
      storage: 10Gi
