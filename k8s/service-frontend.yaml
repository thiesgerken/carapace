apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: carapace
spec:
  selector:
    app.kubernetes.io/name: carapace
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
