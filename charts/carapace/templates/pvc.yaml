apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-data
  labels:
    {{- include "carapace.labels" . | nindent 4 }}
  {{- with .Values.persistence.data.finalizers }}
  finalizers:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - ReadWriteMany
  {{- if .Values.persistence.data.storageClassName }}
  storageClassName: {{ .Values.persistence.data.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.data.size }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-knowledge
  labels:
    {{- include "carapace.labels" . | nindent 4 }}
  {{- with .Values.persistence.knowledge.finalizers }}
  finalizers:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.persistence.knowledge.storageClassName }}
  storageClassName: {{ .Values.persistence.knowledge.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.knowledge.size }}
